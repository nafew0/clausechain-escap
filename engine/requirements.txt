# Core runtime (mirrors pyproject [project.dependencies])
pydantic>=2.7,<3
pyyaml>=6.0,<7
httpx>=0.27,<1
pymupdf>=1.24,<2
numpy>=1.26,<3
# Tests
pytest>=8.0,<9
# Optional extras (uncomment as needed):
# playwright>=1.45            # anti-bot browser fallback for blocked portals
# pytesseract>=0.3.10; pillow>=10.0   # local OCR
# sentence-transformers>=3.0  # Path A BGE-M3 embeddings
# neo4j>=5                    # GRAPH_BACKEND=neo4j
