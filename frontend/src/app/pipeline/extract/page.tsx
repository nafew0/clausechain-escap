import ExtractionWorkspace from '@/views/ExtractionWorkspace'

export const metadata = { title: 'Extraction Workspace — ClauseChain' }

export default function ExtractionWorkspacePage() {
  return <ExtractionWorkspace />
}
